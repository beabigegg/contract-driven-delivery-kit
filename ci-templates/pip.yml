- uses: actions/setup-python@v5
  with:
    python-version: '3.11'
- name: Install dependencies
  run: pip install -r requirements.txt
- name: Lint + Type + Test
  run: |
    ruff check . || true
    mypy src/ || true
    pytest
