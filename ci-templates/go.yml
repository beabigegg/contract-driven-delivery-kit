- uses: actions/setup-go@v5
  with:
    go-version: '1.22'
    cache: true
- name: Lint
  run: |
    go vet ./... || true
    which golangci-lint && golangci-lint run || true
- name: Build + Test
  run: |
    go build ./...
    go test ./...
