- uses: actions/setup-python@v5
  with:
    python-version: '3.11'
- name: Install uv
  run: pip install uv
- name: Install dependencies
  run: uv sync
- name: Lint + Type + Test
  run: |
    uv run ruff check . || true
    uv run mypy src/ || true
    uv run pytest
