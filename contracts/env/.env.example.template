# Copy to .env.example and adapt per repository.
# Never put real secrets in this file.

APP_ENV=development
LOG_LEVEL=INFO

# DEPRECATED (env 0.3.0) — accepted and ignored; removal at env >= 0.5.0.
# The write-block hooks now key off the write target path, not this toggle.
# See contracts/CHANGELOG.md [env 0.3.0].
CDD_ACCEPTANCE_WRITE_STRICT=0
