{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Environment Contract Schema",
  "type": "object",
  "properties": {
    "CDD_ACCEPTANCE_WRITE_STRICT": {
      "type": "string",
      "enum": ["0", "1"],
      "default": "0",
      "description": "Advisory (0, default) vs hard-block (1) mode for pre-tool-use-acceptance-write.sh, the PreToolUse hook guarding specs/changes/<id>/acceptance.yml from agent Edit/Write/MultiEdit. Mirrors CDD_CONTRACT_WRITE_STRICT. Armed via `install-agent-hooks --acceptance-write`."
    }
  },
  "additionalProperties": true
}
